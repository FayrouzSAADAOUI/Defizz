# ── Config locale pour développement ─────────────────────────
# NE PAS committer ce fichier avec de vraies valeurs en prod

PORT=4000
NODE_ENV=development
LOG_LEVEL=debug

# Base de données (docker-compose ou local)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=defizz
DB_USER=postgres
DB_PASSWORD=postgres

# JWT
JWT_SECRET=dev-secret-local-changer-en-prod
JWT_EXPIRES=7d
